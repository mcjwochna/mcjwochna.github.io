---
---
<div class="w-full max-w-6xl mx-auto px-4 py-20">
    <div class="text-center mb-16">
        <h2 class="text-4xl lg:text-5xl font-black mb-4 tracking-tighter">I like where this is headed.</h2>
        <p class="text-text-secondary text-lg max-w-2xl mx-auto">
            Let's talk, type or text.<br><br>
            You can fill out the form below or shoot me using my <b>contact details.</b>
        </p>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 lg:gap-12 items-start">
        
        <!-- LEFT SIDE: Contact Form -->
        <form id="contact-form" class="glass-card p-8 lg:p-10 rounded-3xl space-y-4">
            <!-- Honeypot - hidden from users, visible to bots -->
            <input type="text" name="website" class="hidden" tabindex="-1" autocomplete="off">
            
            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                <div>
                    <input 
                        type="text" 
                        name="name"
                        required
                        minlength="2"
                        maxlength="100"
                        class="w-full bg-white/50 dark:bg-black/20 border border-gray-200 dark:border-white/10 rounded-xl px-4 py-3 focus:outline-none focus:ring-2 focus:ring-accent-primary focus:bg-white dark:focus:bg-black/40 transition-all placeholder:text-gray-400" 
                        placeholder="Your name" 
                    />
                </div>
                <div>
                    <input 
                        type="email" 
                        name="email"
                        required
                        class="w-full bg-white/50 dark:bg-black/20 border border-gray-200 dark:border-white/10 rounded-xl px-4 py-3 focus:outline-none focus:ring-2 focus:ring-accent-primary focus:bg-white dark:focus:bg-black/40 transition-all placeholder:text-gray-400" 
                        placeholder="Your email" 
                    />
                </div>
            </div>
            
            <input 
                type="text" 
                name="subject"
                maxlength="200"
                class="w-full bg-white/50 dark:bg-black/20 border border-gray-200 dark:border-white/10 rounded-xl px-4 py-3 focus:outline-none focus:ring-2 focus:ring-accent-primary focus:bg-white dark:focus:bg-black/40 transition-all placeholder:text-gray-400" 
                placeholder="Message subject" 
            />
            
            <textarea 
                name="message"
                required
                minlength="10"
                maxlength="5000"
                rows="5" 
                class="w-full bg-white/50 dark:bg-black/20 border border-gray-200 dark:border-white/10 rounded-xl px-4 py-3 focus:outline-none focus:ring-2 focus:ring-accent-primary focus:bg-white dark:focus:bg-black/40 transition-all placeholder:text-gray-400 resize-none" 
                placeholder="Hi Maciej, I'd like to discuss a potential Data Engineering project..."
            ></textarea>
            
            <!-- Status message -->
            <div id="form-status" class="hidden rounded-xl px-4 py-3 text-sm font-medium"></div>
            
            <button 
                type="submit" 
                id="submit-btn"
                class="w-full bg-accent-primary text-white font-bold py-4 rounded-xl uppercase tracking-widest hover:bg-accent-primary/90 hover:scale-[1.02] active:scale-95 transition-all shadow-lg shadow-accent-primary/20 disabled:opacity-50 disabled:cursor-not-allowed disabled:hover:scale-100"
            >
                <span id="btn-text">Send Message</span>
                <span id="btn-loading" class="hidden">Sending...</span>
            </button>
        </form>

        <!-- RIGHT SIDE: Contact Details -->
        <div class="glass-card p-8 lg:p-10 rounded-3xl flex flex-col justify-center gap-8 text-left h-full">
            <div>
                <h3 class="text-2xl font-bold mb-2">Contact Details</h3>
                <p class="text-text-secondary text-sm">I'm available for freelance projects and long-term collaboration.</p>
            </div>
            
            <div class="space-y-6">
                <!-- Phone -->
                <div class="flex items-start gap-5 group">
                    <div class="w-12 h-12 rounded-2xl bg-accent-primary/10 flex items-center justify-center text-accent-primary group-hover:scale-110 transition-transform duration-300">
                        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
                        </svg>
                    </div>
                    <div>
                        <p class="text-xs uppercase font-bold text-text-secondary tracking-widest mb-1">Phone</p>
                        <a href="tel:+48603234834" class="text-lg font-bold hover:text-accent-primary transition-colors">+48 603 234 834</a>
                    </div>
                </div>

                <!-- Email -->
                <div class="flex items-start gap-5 group">
                    <div class="w-12 h-12 rounded-2xl bg-accent-primary/10 flex items-center justify-center text-accent-primary group-hover:scale-110 transition-transform duration-300">
                        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
                        </svg>
                    </div>
                    <div>
                        <p class="text-xs uppercase font-bold text-text-secondary tracking-widest mb-1">Email</p>
                        <!-- ========================================
                             EMAIL_CONFIG - Change email here
                             ======================================== -->
                        <a href="mailto:kontakt@mwochna.pl" class="text-lg font-bold hover:text-accent-primary transition-colors">kontakt@mwochna.pl</a>
                        <!-- Alternative: kontakt@in2data.pl -->
                        <!-- ======================================== -->
                    </div>
                </div>
                
                <!-- LinkedIn -->
                <div class="flex items-start gap-5 group">
                    <div class="w-12 h-12 rounded-2xl bg-accent-primary/10 flex items-center justify-center text-accent-primary group-hover:scale-110 transition-transform duration-300">
                        <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                            <path d="M19 0h-14c-2.761 0-5 2.239-5 5v14c0 2.761 2.239 5 5 5h14c2.762 0 5-2.239 5-5v-14c0-2.761-2.238-5-5-5zm-11 19h-3v-11h3v11zm-1.5-12.268c-.966 0-1.75-.79-1.75-1.764s.784-1.764 1.75-1.764 1.75.79 1.75 1.764-.783 1.764-1.75 1.764zm13.5 12.268h-3v-5.604c0-3.368-4-3.113-4 0v5.604h-3v-11h3v1.765c1.396-2.586 7-2.777 7 2.476v6.759z"/>
                        </svg>
                    </div>
                    <div>
                        <p class="text-xs uppercase font-bold text-text-secondary tracking-widest mb-1">LinkedIn</p>
                        <!-- ========================================
                             LINKEDIN_CONFIG - Change LinkedIn URL here
                             ======================================== -->
                        <a href="https://linkedin.com/in/wochnamaciej" target="_blank" rel="noopener" class="text-lg font-bold hover:text-accent-primary transition-colors">/in/wochnamaciej</a>
                        <!-- ======================================== -->
                    </div>
                </div>

                <!-- GitHub -->
                <div class="flex items-start gap-5 group">
                    <div class="w-12 h-12 rounded-2xl bg-accent-primary/10 flex items-center justify-center text-accent-primary group-hover:scale-110 transition-transform duration-300">
                        <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                            <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
                        </svg>
                    </div>
                    <div>
                        <p class="text-xs uppercase font-bold text-text-secondary tracking-widest mb-1">GitHub</p>
                        <a href="https://github.com/mcjwochna" target="_blank" rel="noopener" class="text-lg font-bold hover:text-accent-primary transition-colors">/mcjwochna</a>
                    </div>
                </div>
            </div>
        </div>

    </div>
</div>

<style>
    .glass-card {
        background: rgba(255, 255, 255, 0.6);
        backdrop-filter: blur(20px);
        -webkit-backdrop-filter: blur(20px);
        border: 1px solid rgba(255, 255, 255, 0.6);
        box-shadow: 0 10px 40px -10px rgba(0, 0, 0, 0.05);
    }
    
    :global(.dark) .glass-card {
        background: rgba(20, 20, 20, 0.4);
        border: 1px solid rgba(255, 255, 255, 0.08);
        box-shadow: 0 10px 40px -10px rgba(0, 0, 0, 0.5);
    }
    
    .hidden {
        display: none !important;
    }
    
    #form-status.success {
        background: rgba(16, 185, 129, 0.1);
        border: 1px solid rgba(16, 185, 129, 0.3);
        color: rgb(16, 185, 129);
    }
    
    #form-status.error {
        background: rgba(239, 68, 68, 0.1);
        border: 1px solid rgba(239, 68, 68, 0.3);
        color: rgb(239, 68, 68);
    }
</style>

<script>
    const form = document.getElementById('contact-form') as HTMLFormElement;
    const submitBtn = document.getElementById('submit-btn') as HTMLButtonElement;
    const btnText = document.getElementById('btn-text') as HTMLSpanElement;
    const btnLoading = document.getElementById('btn-loading') as HTMLSpanElement;
    const formStatus = document.getElementById('form-status') as HTMLDivElement;
    
    // API endpoint - change to production URL after deployment
    const API_URL = 'https://in2data.pl/contact.php';
    
    function setLoading(loading: boolean): void {
        submitBtn.disabled = loading;
        btnText.classList.toggle('hidden', loading);
        btnLoading.classList.toggle('hidden', !loading);
    }
    
    function showStatus(type: 'success' | 'error', message: string): void {
        formStatus.textContent = message;
        formStatus.className = `rounded-xl px-4 py-3 text-sm font-medium ${type}`;
        formStatus.classList.remove('hidden');
    }
    
    function hideStatus(): void {
        formStatus.classList.add('hidden');
    }
    
    form.addEventListener('submit', async (e: Event) => {
        e.preventDefault();
        hideStatus();
        setLoading(true);
        
        const formData = new FormData(form);
        const data = {
            name: formData.get('name') as string,
            email: formData.get('email') as string,
            subject: formData.get('subject') as string || 'Website inquiry',
            message: formData.get('message') as string,
            website: formData.get('website') as string, // honeypot
        };
        
        try {
            const response = await fetch(API_URL, {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify(data),
            });
            
            const result = await response.json();
            
            if (response.ok && result.status === 'success') {
                showStatus('success', result.message);
                form.reset();
            } else {
                showStatus('error', result.message || 'An error occurred. Please try again.');
            }
        } catch (error) {
            console.error('Contact form error:', error);
            showStatus('error', 'Unable to connect to server. Please check your internet connection.');
        } finally {
            setLoading(false);
        }
    });
</script>