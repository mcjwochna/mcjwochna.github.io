---
import { principles, bio } from '../data/about';
---
<div>
    <!-- Section header -->
    <div class="flex items-center gap-4 mb-16">
        <span class="text-accent-primary font-mono text-sm">01. About</span>
        <div class="h-[1px] bg-bg-secondary flex-1"></div>
    </div>
    
    <!-- Bio text -->
    <div class="max-w-3xl mb-20">
        <p class="text-text-secondary text-lg leading-relaxed">
            {bio}
        </p>
    </div>
    
    <!-- Principles subheader -->
    <div class="flex items-center gap-4 mb-12">
        <span class="text-accent-primary font-mono text-sm">My Principles</span>
        <div class="h-[1px] bg-bg-secondary flex-1"></div>
    </div>
    
    <!-- Principles grid -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-12">
        {principles.map((p, index) => (
            <div class="group">
                <span class="text-accent-primary/20 font-black text-6xl block mb-6">
                    0{index + 1}
                </span>
                <h3 class="text-xl font-bold mb-4 italic">{p.title}</h3>
                <p class="text-text-secondary leading-relaxed">
                    {p.body}
                </p>
            </div>
        ))}
    </div>
</div>